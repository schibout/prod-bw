<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>FSL_Is_Active__c</fullName>
    <description>Si la case est cochée, elle indique que le plan de maintenance est en cours de réalisation.</description>
    <externalId>false</externalId>
    <formula>AND(IF( EndDate &gt;= DateValue(now()), True, False),IF( StartDate &lt;= DateValue(now()), True, False))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Si la case est cochée, elle indique que le plan de maintenance est en cours de réalisation.</inlineHelpText>
    <label>Is Active</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
